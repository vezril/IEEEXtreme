#!/usr/bin/env python

def find_smallest(c):
    ml = 9999999999999999999
    s = None

    for key in c:
        if len(c[key]) < ml:
            s = key
    return key
        
        
def decrypt(p, c)

if __name__ == "__main__":
    N = 2
    pt = ['case','simple']
    ct = ['AJWHPU', 'GXAU']
    
    p = {}
    c = {}
    for plain in pt:
        try:
            p[len(plain)].append(plain)
        except:
            p[len(plain)] = [plain]
        
    for cipher in ct:
        c[len(cipher)] = cipher
        

            

